<table align="center" class="table table-striped">
    <tr>
            <th>User Id</th>
            <th>Username</th>
            <th>Name</th>
            <th>Email</th>
    </tr>
    <% @users.each do |user| %>
        
        <tr>
            <td><%= user.id %></td>
            <td><%= link_to user.username, user_path(user) %></td>
            <td><%= user.name %></td>
            <td><%= user.email %></td>
        </tr>
    <% end %>
</table>